# Data Collection Checklist

Before loading data, record the source URL, access date, file format, target column, and any restrictions on use.

The original file should remain unchanged in `data/raw/`; cleaning outputs belong in `data/processed/`.
