# Tests

Focused checks will be added here as data preparation and prediction logic become executable.

Tests should verify assumptions about inputs and outputs instead of only checking that code runs.
